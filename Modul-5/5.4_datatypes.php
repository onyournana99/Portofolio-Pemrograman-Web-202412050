<?php
    $judul_buku = "Dasar-Dasar Web"; // String
    $harga = 95000;                  // Integer
    $rating = 4.5;                   // Float
    $tersedia = true;                // Boolean

    // Memeriksa tipe dan nilai dari setiap variabel menggunakan var_dump()
    var_dump($judul_buku);
    var_dump($harga);
    var_dump($rating);
    var_dump($tersedia);
?>